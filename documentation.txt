--Dashboard back

1. Connection db creacion de model realizada. 
2. Implementar logica creacion de usuario. Cada uno tendra roles, el usuario principal se creeara de forma que sea el unico capaz de general roles en los usurios consumidores de forma cliente . DONEE
3. Creacion de autenticacion con email y generando token. Seria bueno que el token devuelva tambien el rol asi evitamos una busqueda en la base de datos. 
Continuar con el paso 4, ya realice endpoint que recoge mi data user, ya realice el middleware auth y demas 
4. Creacion de modelo producto el cual genere luego proximas estadisticas, analizis de venta y demas. Esto solo podra realizarlo el usuario admin
·$·$""·$"  REMEMBER.
CREAR CRUD COMPLETO DEL MODELO USER ASI PUEDO MODIFICAR DATOS DEL USER  done
USERS ====== EMPLEADOS COMPAÑEROS Y UN ADMIN DE PRINCIPIO
·$·$·$·$·PRODUCTOS·$·$·$·$·$
EN CREATE PRODUCT: aplicar logica de busqueda de ese producto con ese nombre para que si ese producto ya existe no lo cree de nuevo sino que sume uno al stock del que ya hay  DONEE

CREAR CRUD DE PRODUCT  DONE!!!
Ya cree el supplier model, ahora tengo que continuar con  el crud de los supplier ;) :D
Crear delete supplier /(&(#/"!$&(!/"$%!/("$))))DONEEE

DEJAR LOS ENDPOINTS BIEN REALIZADOS CON EL CACH DE ERRORES CON BUENAS PRACTICAS COMO ESTA EN EL DELETESUPPLIER %$&%$%&$%&$%&& DONEEEE!

NEXT!$#$#
continuar con proximos modelos categorie and ventas


TENGO QUE AGREGAR AUTH MIDDLEWARE A TODOS LOS ENDPOINTS PARA EL ROL CLIENTE TAMBIEN, QUE PUEDA VER LOS PRODUCTOS
 if (!req.user) {
      res.status(401).send({ error: "User not found" });
      return new Error("User not found");
    }
    
    continuar con modelo ventas :DDD
    LUEGO CONTINUAR CON EL MODELO CATEGORIES



!!!!!!!!!!!!!!!!!CONTINUAR CON ESTO


/&(&()/))=()= CREAR DELETE DE CATEGORIE
%&$&&%$%& TENGO QUE AGREGAR EL MIDDLEWARE QUE SE FIJE QUE ROLE TIENE ASI NO REPITO ESA LOGICA EN TODOS LOS ENDPOINTS. done


CREAR middleware QUE SE ENCARGUE DE VERIFICAR ROL DEL USER 'ADMIN' OR 'CLIENT' done

ESTO PROXIMO HACER Y CHEQUEAR QUE OTROS MODELOS CREAR ;) 

modelo sale ver como crear este endpont deberia ser un webhook que el gateway de pago acceda para generar la compra ;)



TERMINAR CREATE SALES ESTOY CON EL BODY DE POSTMAN


FIJARME SI EN EL CREATE SALE USAR QUERY O PARAMS PARA TRAER ID DEL P´RODUCTO CON EL CUAL PODEMOS CREAR EL SALE 



TENGO QUE ARREGLAR EL MIDDLEWARE QUE REVISA SI ES AUTH, PORQUE NO ESTA FUNCIONANDO BIEN ME DEJA AVANZAR done !

crear endpoint para cambiar roles de usuarios, retornar lista de usuarios client done !

ARREGLAR MIDDLEWARES DE AUTH Y VERIFICACION ADMIN    done !


implementar nuevos endpoints tasks a frontendm como tambien el cambio de rol
crear modelo notificacion para que aparezca una notificacion cuando creo una tarea o cualquier otra cosa, creacion de producto o cualquier modificacion. 
Crear tambien listado de usuarios en el front para poder ver que usuarios hay y cambiar roles si soy admin. Tambien crear listado de suppliers crear crud alli y acomodar las lecturas de los canvas para que me muestre las estadisticas correctamente

ver si en notifications es necesario agregar si es task o supplier o alguna otra cosa, ya tengo la referencia de id


NOTIFICACIONES POR AHORA SOLO FUNCIONA CON EL CRUD DE TASKS